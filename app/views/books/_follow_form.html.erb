<div id="follow_form">
  <% if current_user.following_book? @book %>
    <%= render "unfollow", follow_book: follow_book %>
  <% else %>
    <%= render "follow", follow_book: follow_book %>
  <% end %>
</div>
