<%= form_for like_book, remote: true do |f| %>
  <div><%= hidden_field_tag :book_id, @book.id %></div>
  <%= f.button type: "submit", class: "margin5" do %>
    <i class="far fa-thumbs-up"><%= t "users.like" %></i>
  <% end %>
<% end %>
